# 项目协作规则

## 中文注释规范

- 所有新增或修改的项目自有 C# 类型，包括类、记录、结构、接口和枚举，都必须在声明前编写中文 XML `<summary>` 注释。
- 注释必须说明类型的职责、保护的约束、所在边界或采用当前设计的原因，不能只复述类型名称或代码语法。
- 公共扩展点、认证授权、模型调用、工具策略、外部接口、敏感数据处理和持久化代码，必须明确说明安全边界与失败约束。
- 测试类和测试替身的注释应说明它验证或模拟的行为，不能使用“测试某类”作为唯一信息。
- 修改已有代码时同步检查相邻注释是否仍与实际行为一致；过期注释与缺失注释同样视为质量问题。
- `RepositorySafetyArchitectureTests` 提供逐类型中文 XML 注释门禁，内容是否准确、具体仍由代码审查确认。
